# CreatorProof build identity

- Product version: **0.9.2**
- Build signature: **SEMANTIC-SAFETY-SCOPE-2026.08.10**
- Edition: **Truthful Scope + Policy Safety**

Visible acceptance markers:

1. `CreatorProof v0.9.2` appears in the status bar and case header.
2. Every completed case displays a typed source-coverage statement before its bottom line.
3. Coverage reports eligible, nominated, verified, omitted, and failed reference counts plus a
   catalog version and snapshot digest.
4. Only `COMPLETE` scope can support `NO_MATCH_IN_CHECKED_SOURCES`; every incomplete status is
   visibly review-only.
5. AI origin, same-work copy, and creator-profile resemblance remain independent evidence lanes.
6. AI-origin policy effect is shown as `DISABLED`, `INFORMATIONAL`, or `REQUIRED`; informational is
   the default and cannot independently change policy.
7. Registration exposes claim state. Only `CORROBORATED` claims can authorize recorded use.
8. Reusing an idempotency key with changed candidate bytes, catalog, or intended use returns a typed
   `409 IDEMPOTENCY_PAYLOAD_MISMATCH`.
9. Every policy result records policy version, policy inputs, and reason codes.
10. Creator-profile support is labelled catalog-relative empirical support, not conformal coverage,
    attribution, or a legal conclusion.
11. Uncalibrated passive AI detectors cannot emit a high-authority likely-AI outcome.
12. Copy regions remain fail-closed: rejected geometry exposes no invented correspondence or region.
13. Every index is labelled **not probability** and no UI element declares legal infringement.
14. A local Merkle receipt is visibly labelled **not blockchain**. Only a mined, validated EAS
    receipt is shown as a public-chain anchor.
15. A local scan POST returns a scan ID without waiting for detection to finish, and progress remains
    recoverable by scan ID.
16. Proof anchoring cannot delay or overturn the completed core evidence result.
17. The release gate is 85 backend tests, Ruff/format validation, TypeScript, and a production Next.js build.

The active style stack is CSD (when locally available) plus CSD+ catalog readout, transparent
mark-making diagnostics, SSCD content-confound control, catalog-relative empirical support, and
corroborated fusion. The default fallback remains diagnostic-only and cannot trigger creator
attribution. The AI-origin stack is C2PA-first, official-preprocessing-correct, multi-crop,
evidence-family-aware, calibratable, abstaining, and independently supplemented by visible-label
OCR. The 0–100 origin readouts are signal and evidence-quality indexes, not probabilities.
