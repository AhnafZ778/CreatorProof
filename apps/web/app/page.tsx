/**
 * App Router requires a page at `/`. Marketing content is served via middleware rewrite
 * to `/landing.html`. Artist and User portals live at `/artist` and `/user`.
 */
export default function Home() {
  return null;
}
