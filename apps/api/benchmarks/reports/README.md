# Benchmark reports

No empirical benchmark report is checked in yet. Add only reports produced from a
locked ModelBundle and authorized manifest set. Validate each report before using a
metric in a demo, submission, chart, or claim.
